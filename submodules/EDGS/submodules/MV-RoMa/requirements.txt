# Step 1: Install PyTorch (CUDA 11.8 build) — must be done separately
# pip install torch==2.5.1+cu118 torchvision==0.20.1+cu118 torchaudio==2.5.1+cu118 \
#     --index-url https://download.pytorch.org/whl/cu118

# Step 2: Install uniception + uniflowmatch from UFM repo (submodule-based)
# git clone --recursive https://github.com/UniFlowMatch/UFM.git
# cd UFM/UniCeption && git checkout f839559 && pip install -e . && cd ..
# pip install -e .

# Step 3: Install remaining dependencies below

# Vision / geometry
kornia==0.8.1
timm==1.0.15
opencv-python==4.11.0.86
albumentations==2.0.7

# Tensor utilities
einops==0.8.1
numpy==1.26.4

# Model hub / config
huggingface-hub==0.36.0
omegaconf==2.3.0

# Data
h5py==3.13.0
datasets==4.0.0

# Training utilities
accelerate==1.8.1

# Logging / viz
loguru==0.7.3
matplotlib==3.10.3
seaborn==0.13.2
tqdm==4.67.3
tensorboard==2.19.0

# System (install via apt if missing):
# apt-get install -y libgl1-mesa-glx libglib2.0-0 tmux vim git
