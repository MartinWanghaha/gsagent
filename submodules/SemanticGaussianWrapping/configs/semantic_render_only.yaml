_base_: default.yaml

# Learns Gaga embeddings/decoder, but disables semantic geometry and meshing.
loss:
  lambda_manifold: 0.0
  lambda_geometry: 0.0
  lambda_sh: 0.0
  lambda_surface: 0.0
  lambda_mesh: 0.0

density:
  semantic_weight: 0.0
  boundary_weight: 0.0
  geometry_weight: 0.0

surface:
  enabled: false
